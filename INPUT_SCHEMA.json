{
  "title": "Apollo Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "country": {
      "title": "Country",
      "editor": "textfield",
      "type": "string",
      "default": "United States",
      "description": "Enter the country to target for scraping employees. Default is United States."
    },
    "region": {
      "title": "State or Region",
      "editor": "textfield",
      "type": "string",
      "description": "Optional. Enter a specific state or region within the selected country."
    },
    "companySizes": {
      "title": "Company Size Ranges",
      "editor": "multiselect",
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "1-10",
          "11-50",
          "51-200",
          "201-500",
          "501-1000",
          "1001-5000",
          "5001-10000",
          "10000+"
        ]
      },
      "default": ["11-50", "51-200"],
      "description": "Select one or more company size ranges to filter results."
    },
    "onlyValidEmails": {
      "title": "Only include verified emails (no catch-all)",
      "type": "boolean",
      "editor": "checkbox",
      "default": true,
      "description": "Enable this to exclude catch-all or unverified emails."
    }
  },
  "required": ["country"]
}